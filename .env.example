# Copy to .env and fill in. Never commit the real .env — it holds a live
# credential. See development/03_SECURITY.md.
#
# Fine-grained PAT, "Administration: write" permission, scoped to exactly the
# repos listed in config/repos.yaml. Not a classic PAT with blanket `repo`
# scope.
GH_PAT=
